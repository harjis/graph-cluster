export const GRID_SIZE = 20;
// distance by which the grid is shifted up and to the left (make sure that this is less than GRID_SIZE)
export const GRID_SHIFT = 10;

export const DEFAULT_NODE_WIDTH = 8 * GRID_SIZE;
export const DEFAULT_NODE_HEIGHT = 3 * GRID_SIZE;
